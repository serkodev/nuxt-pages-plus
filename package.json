{
  "name": "nuxt-pages-plus",
  "version": "1.0.0-beta.4",
  "description": "A Nuxt module that enables complex routing for Nuxt Pages.",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/serkodev/nuxt-pages-plus.git"
  },
  "license": "MIT",
  "type": "module",
  "exports": {
    ".": {
      "types": "./dist/types.d.mts",
      "import": "./dist/module.mjs",
      "require": "./dist/module.mjs"
    }
  },
  "main": "./dist/module.mjs",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "nuxt-module-build build",
    "dev": "nuxi dev",
    "dev:build": "nuxi build",
    "dev:prepare": "nuxt-module-build build --stub && nuxt-module-build prepare && nuxi prepare examples/base",
    "play": "nuxi dev playground",
    "play:build": "nuxi build playground",
    "docs:dev": "pnpm --dir docs dev",
    "docs:build": "pnpm --dir docs build",
    "docs:deploy": "pnpm dev:prepare && pnpm docs:build",
    "prepublishOnly": "pnpm lint && pnpm test && pnpm build && bumpp",
    "release": "pnpm publish",
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "test": "vitest run",
    "test:watch": "vitest watch",
    "typecheck": "vue-tsc --noEmit -p tsconfig.typecheck.json",
    "test:matrix": "pnpm build && node test/matrix/run.mjs",
    "prepare:types": "pnpm --filter \"./examples/**\" --filter \"./playground\" prepare:types",
    "prepare": "nuxt-module-build prepare"
  },
  "dependencies": {
    "@nuxt/kit": "catalog:nuxt",
    "defu": "^6.1.7",
    "ufo": "^1.6.4"
  },
  "devDependencies": {
    "@antfu/eslint-config": "catalog:dev",
    "@nuxt/eslint-config": "catalog:dev",
    "@nuxt/module-builder": "catalog:nuxt",
    "@nuxt/schema": "catalog:nuxt",
    "@nuxt/test-utils": "catalog:nuxt",
    "@nuxt/vite-builder": "catalog:nuxt",
    "@types/node": "^24.13.3",
    "bumpp": "^12.2.0",
    "eslint": "catalog:dev",
    "nuxt": "catalog:nuxt",
    "playwright-core": "^1.62.1",
    "typescript": "catalog:dev",
    "vitest": "^4.1.10",
    "vue-router": "catalog:",
    "vue-tsc": "^3.3.9"
  },
  "peerDependencies": {
    "nuxt": ">=3.16.0"
  },
  "packageManager": "pnpm@11.21.0"
}
