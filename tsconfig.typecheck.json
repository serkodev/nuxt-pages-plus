{
  // scoped typecheck for the module itself (fixtures/apps have their own Nuxt contexts)
  "extends": "./.nuxt/tsconfig.json",
  "exclude": [
    "node_modules",
    "dist",
    ".data",
    "docs",
    "examples",
    "playground",
    "test/e2e/fixtures",
    "test/matrix/fixture"
  ]
}
